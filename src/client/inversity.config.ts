import { Container } from 'inversify';
import { AIR_NAVIGATION_API, AirNavigationApi } from './services/airNavigationApi';

// const container = new Container({ skipBaseClassChecks: true, });
// container.bind(AIR_NAVIGATION_API).to(AirNavigationApi).inSingletonScope();

// export { container };